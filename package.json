{
  "name": "single-spa-portal-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "node install_all.js",
    "start": "node start_all.js",
    "build": "npm run build:ilc && npm run build:registry",
    "build:ilc": "cd ./ilc/ && npm run build",
    "build:registry": "cd ./registry/ && npm run compile",
    "postinstall": "node postinstall.js"
  },
  "author": "Vladlen Fedosov",
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "4.0.1"
  }
}
